# Codex 프롬프트 엔지니어링 로그

**이 세션(Claude Code)의 역할이 바뀌었다 (2026-07-27부터, 별도 지시 전까지 유지).**
더 이상 이 저장소의 소스 코드를 직접 작성/수정하지 않는다. 대신:

- 사용자가 다른(Codex) 세션에서 진행 중인 작업 상태나 "뭐가 안 됨/이상함" 같은
  추상적인 피드백을 말로 전달하면,
- 이 세션은 저장소의 **현재 실제 코드 상태를 계속 추적**해서 그 피드백을
  구체적인 파일/증상에 근거를 붙이고,
- Codex 세션에 그대로 붙여넣을 수 있는 **완성된 프롬프트**를 작성해 응답한다.

`docs/ai-usage/session-log.md`(매 턴 AI 활용 요약, 대회 제출용)와는
**별개 문서**다 — 이 로그는 "무슨 프롬프트를 왜 만들었는지"의 기록이고,
저 로그는 이 저장소에 대한 AI 협업 전체 서사다. 하네스의 다른 규칙
(`docs/dev-wiki/contract.md`의 The One Rule / backlog.md 등)은 그대로
유지된다 — 이 세션이 실제로 파일을 바꾸는 일은 없겠지만, 만약 바뀐다면
그 규칙을 따른다.

## 형식

턴마다:
- **사용자 상황/요청 원문 요약** (가능하면 원문 인용 포함)
- **확인한 현재 코드 상태** (파일/커밋/증상 근거)
- **작성한 프롬프트** (전체 텍스트, Codex에 그대로 붙여넣을 수 있는 형태)

---

## 2026-07-27 (1) — 모드 전환 + 현재 상태 재확인

- **사용자 지시 요약**: 이 세션은 이제부터 프롬프트 엔지니어링 전용으로
  전환. 하네스(백로그 등)는 유지하되, 대화 요약 저장은 이 별도 문서로
  옮길 것. 사용자가 앞으로 추상적인 문제 상황("뭐가 안 됨", "뭐가
  이상함")을 말하면, 이 세션이 현재 소스 상태를 근거로 Codex용 프롬프트를
  작성. 별도 지시 전까지 이 모드 유지. 먼저 그동안 추가 개발된 내용을
  확인해달라고 요청.
- **확인한 현재 코드 상태** (2026-07-27, 브랜치 `terrain-prototype-central`,
  최신 커밋 `91fb6c0`):
  - 오디오: 이전 턴에 만든 독립 프로토타입(`f57303b`)이 Codex 세션에
    의해 실제 게임에 완전히 통합됨(`docs/dev-wiki/audio-integration-validation.md`).
    `BootScene`이 `getAudioSystem()` 싱글턴을 초기화, Boot=menu/
    시작=preparation/웨이브=battle-low/전투 강도에 따라 battle-high
    (엔게이지 유닛≥6 또는 발사체≥3 또는 최근 공격≥7 시 진입, 최소
    6초 유지, engaged≤2 & 발사체=0 & 공격≤1일 때만 저강도 복귀 —
    히스테리시스로 전투 판정에 영향 안 줌), 요새 체력 35% 이하에서
    경고 레이어(1.8초, 12초 쿨다운), 승패에 따라 victory/defeat,
    재시작 시 director reset. SFX 32종 중 고빈도(근접/원거리/피격/사망/
    타워)는 실제 전투 이벤트에 연결됨(reduced 모드가 기본, 고빈도
    요청 1/3만 샘플링). 공간 음향(`spatialAudio.ts`, 카메라 상대
    거리 기반 볼륨+팬)도 추가됨. 설정 패널(`src/ui/AudioSettingsPanel.ts`,
    HUD "소리" 버튼 또는 Esc)도 신규. 여전히 실제 오디오 파일은
    0개(전부 `missingAsset: true`, 합성 음으로 대체) — 이 부분은
    내가 처음 설계한 그대로 유지됨.
  - 비주얼/유닛: 최신 커밋(`91fb6c0`, "improve unit readability and
    fortress point rules")으로 유닛 크기 프리셋이 `compact/recommended/
    large` 3종으로 확정되고 `recommended`가 기본값. 타워 높이(일반
    144px/고정 요새 162px), 이동 방향에 따른 좌우 반전, 공격 시 방향
    잠금, 고정 요새 UI 규칙(만피 시 조작 없음/피해 시 수리만/파괴 시
    재건만, 해체·용도변경 불가), 일반 거점 UI(소유 시 요새/보급/조달
    선택+해체) 등이 `docs/dev-wiki/terrain-prototype-v3-validation.md`에
    상세 기록됨.
  - 지형: V1→V2→V3까지 반복. V2는 중앙 거점 원형 패드만 불투명 도로
    픽셀로 교체(`terrain-prototype-v2-validation.md`), V3는 이걸 전체
    맵으로 확장하지 않고 유닛 크기/타워 높이/방향/공격 표현/고정
    요새 규칙만 추가 검증(`terrain-prototype-v3-validation.md`). 즉
    **중앙 거점 지형 개선은 아직 전체 레인에 적용 안 됨** — 여전히
    사용자 판단 대기 중인 항목으로 보임.
  - `project_development.md`(단일 현황판)는 **갱신이 안 돼서 stale함**
    — 여전히 `DungeonScene`/필드 자동전투를 "현재 상태"로 기술하고
    있고, 오디오 통합이나 유닛 가독성 커밋, terrain v3는 전혀 반영 안 됨.
    다음에 Codex 쪽에 문서 정리를 지시할 기회가 있으면 이 부분도
    언급할 것.
- **작성한 프롬프트**: 없음(이번 턴은 상황 파악만 — 사용자의 다음
  구체적 지시를 기다림).

## 2026-07-27 (2) — 밸런스/오디오/입력/지형 5건 피드백 → Codex 프롬프트

- **사용자 지시 요약**: 게임 스크린샷(레인 전투 화면, 고정 요새·거점
  UI, 웨이브 예고 텍스트 등이 보임)을 첨부하며 5가지 문제 제기. 사용자가
  "첨부된 스크린샷은 실제 게임 수정하는 프롬프트 쪽에도 직접 넣을
  예정이니 감안하여 작성하도록 해"라고 명시 — 즉 스크린샷은 사용자가
  Codex 프롬프트에 직접 첨부할 것이므로, 내가 작성하는 프롬프트 텍스트는
  "첨부된 스크린샷 참고"를 전제로 서술.
  1. **"보급대의 치유량이 너무 높은 듯. 이거 기존에 유닛 5+1일 때를
     토대로 한 건데, 지금은 3+1로 유닛이 많이 줄어든 것을 감안해야 함."**
  2. **"웨이브 생성 텀이 너무 길다. 30초마다 한 번씩으로 해줘."**
  3. **"효과음 등 음악이 생성되기는 했는데 너무 투박하다. 뭔가 좀
     웅장한 느낌/비장하면서도 발전하는 느낌 나면서 싸울 때는 서걱 서걱,
     투곽 투곽 으억 윽 이런 소리도 나고 보급대가 힐 할 때면 치리링
     하는 힐 소리도 났으면 좋겠다. 배경음악 자체도 변경하고."**
  4. **"이 게임은 모바일로도 추후에 될 수 있기 때문에 키보드 클릭과
     같은 방식은 없애고 모든 옵션이 UI로 선택하여 진행할 수 있도록
     해야함"**
  5. **"배경 화면과 오브젝트화된 것들이 아직도 별개로 움직이는데 배경
     화면도 이제 하나의 통짜가 아니라 앞서 여러 타일형을 만들고 그러한
     조합으로 구성되도록 해줘. 배경화면이랑 타일이 스크린샷처럼 분리되어
     떠 있어 보임."**
- **확인한 현재 코드 상태** (근거 조사, 코드는 건드리지 않음):
  1. `src/scenes/LaneBattleScene.ts:286`
     `supply_wagon: { ... healPower: 10 ... }`. `tickSupport()`
     (같은 파일 약 1403~1425행)가 `attackCooldownSec`(보급대는 1.2초)
     마다 사거리 안 부상 아군에게 `healPower`를 나눠 적용. `src/data/unitRosters.ts`
     확인 결과 stone 시대 로스터가 `battleline: [슬링어 1 + 도끼병 2] = 3`
     `+ support: [보급대 1]` — 사용자 말대로 정확히 "3+1". 병력이 5→3으로
     줄면서 같은 총 회복량이 더 적은 인원에 분산 → 개체당 체감 회복량 증가.
  2. `src/data/balance.ts:34` `export const WAVE_INTERVAL_SEC = 90;`.
     단순 값 변경이지만, 같은 파일의
     `INSTANT_WAVE_TOKEN_COOLDOWN_AFTER_WAVE_SEC = 10`과
     `LaneBattleScene.ts:1282`의 `this.enemy.nextWaveInSec > 22`
     조건이 **90초 주기를 전제로 캘리브레이션된 값**이라, 30초로
     줄이면 이 조건이 주기 대부분(30~22초 구간, 즉 처음 8초)에서
     항상 참이 되어 즉시 웨이브 토큰 로직의 의미가 달라짐 — 같이
     점검 필요.
  3. `src/systems/audio/backend.ts`의 `playSynthScore`/`getScoreProfile`
     (BGM, 상태별 bass/chord/lead/pulse 조합)와 `playSynthOneShot`
     (SFX, `SynthProfile.kind`가 `pad|pluck|pulse|noiseHit|sweepUp|
     sweepDown|chime` 6종뿐, 단순 sine/triangle/square 파형 1~2겹).
     **보급대 힐 전용 효과음은 아예 없음** — `tickSupport()`의 힐
     처리 분기(위 1번 참고)에 `this.audio.playSfx(...)` 호출 자체가
     없음(`grep`으로 확인, `LaneBattleScene.ts` 전체 `playSfx` 호출
     목록에 heal 계열 없음). 새 SFX id(예: `sfx.support.heal`)를
     매니페스트에 추가하고 실제로 연결해야 함.
  4. `grep -rn "keyboard\.|addKey|keydown-"` 결과: `AudioSettingsPanel.ts`의
     ESC/M/좌우화살표는 전부 클릭 가능한 버튼·드래그 슬라이더가 이미
     있고 키보드는 추가 단축키일 뿐이라 문제 없음. **진짜 문제는
     `LaneBattleScene.ts:673` `keydown-T`** — 지형 렌더 모드
     (`legacy/prototype/prototype-v2`)를 순환하는데 이건 **키보드가
     유일한 접근 경로**(터치/클릭 UI 없음, URL 쿼리 파라미터로만
     대체 가능). `BootScene.ts`의 스페이스 시작은 `pointerdown`도
     같이 있어 문제 없음. `DungeonScene.ts`의 WASD는 `main.ts`에
     등록 안 된 레거시 씬이라 실제 게임과 무관.
  5. `docs/dev-wiki/terrain-rendering-plan.md`가 이미 정확히 같은
     문제를 진단해뒀음: "플레이 영역 전체가 한 장의 저해상도 배경으로
     렌더링된다"(`lane-battlefield-object-base-v4.png` 한 장, depth 0)
     + "건물 footprint나 지면 점유 셀은 없다". V1~V3 프로토타입은
     **중앙 거점 8x8 패치만** 하이브리드 타일로 교체했고
     (`terrain-prototype-v2-validation.md`의 `balanced/M` 추천안),
     **전체 레인으로는 확장한 적이 없음** — 사용자가 스크린샷에서
     지적한 "배경과 타일이 분리되어 떠 보임" 현상과 정확히 일치.
- **작성한 프롬프트**: 아래 전문, 사용자에게 그대로 전달.

```
저장소 현재 상태를 기준으로 확인한 5가지 이슈를 고쳐줘. 첨부한 스크린샷은
지금 레인 전투 화면(고정 요새·거점 UI, 웨이브 예고 텍스트 포함)이니 5번
항목 판단에 참고해라.

# 공통 원칙

- 확실하지 않은 부분은 추측하지 말고 물어봐라.
- 작업 전 `git status`로 현재 브랜치 상태를 확인하고, 지금 진행 중인 다른
  작업과 겹치는 파일을 건드리게 되면 먼저 알려라.
- 아래 5개는 서로 독립적인 이슈다. 한 항목을 고치다가 다른 항목의 수치나
  로직을 임의로 같이 바꾸지 마라.
- 각 항목 수정 후 `npm run build`, `npm test`가 여전히 통과하는지 확인해라.

---

## 1. 보급대(supply_wagon) 치유량이 로스터 축소를 반영 못 함

`src/scenes/LaneBattleScene.ts`의 `supply_wagon` 스탯(`healPower: 10`,
`attackCooldownSec: 1.2`)과 `tickSupport()`(사거리 안 부상 아군에게
`healPower`를 나눠 적용하는 로직)를 확인해라. 이 수치는 예전 로스터
(전투 유닛 5 + 보급 1)를 기준으로 잡은 값인데, `src/data/unitRosters.ts`의
현재 로스터는 시대별로 전투 유닛이 3(석기 기준 슬링어1+도끼병2)+보급 1로
줄어 있다. 같은 총 회복량이 더 적은 인원에 분산되면서 개체당 체감
회복량이 과도하게 높아졌다.

- 로스터 크기(전투 유닛 수)와 회복량의 관계를 다시 계산해서, 5+1일 때
  느꼈던 밸런스 감각과 비슷해지도록 `healPower`(또는 필요하다면
  `attackCooldownSec`)를 시대별 로스터 크기에 비례해서 조정해라. 정확한
  숫자는 네가 계산해서 제안하고, 하드코딩된 단일 상수보다는 로스터 크기를
  실제로 참조하는 계산식을 쓰는 게 이후 로스터가 또 바뀔 때도 안전하다.
- 다른 시대(bronze/iron_early/iron_mid/iron_late) 로스터도 전투 유닛 수가
  전부 3명이니 같은 기준으로 맞춰라.
- 조정 후 실제 플레이(또는 시뮬레이션 로그)로 "보급대 없을 때 vs 있을 때"
  생존 시간 차이가 과거보다 합리적인 수준인지 확인해라.

## 2. 웨이브 생성 간격을 90초 → 30초로

`src/data/balance.ts`의 `WAVE_INTERVAL_SEC = 90`을 `30`으로 바꿔라.

**주의**: 같은 파일의 `INSTANT_WAVE_TOKEN_COOLDOWN_AFTER_WAVE_SEC = 10`과
`LaneBattleScene.ts`의 `this.enemy.nextWaveInSec > 22`(즉시 웨이브 토큰
발급 조건, 약 1282행 근처)는 90초 주기를 전제로 잡은 값으로 보인다. 90초
기준으로는 주기의 마지막 78~90초(약 13% 구간)에서만 조건이 참이 되도록
설계된 것 같은데, 30초로 줄이면 이 조건이 주기 대부분에서 참이 되어 즉시
웨이브 토큰 로직의 의도가 깨질 수 있다. `WAVE_INTERVAL_SEC`을 바꿀 때
이 임계값들도 같은 비율로(또는 원래 의도한 "주기 막바지"라는 의미가
유지되도록) 같이 조정해라. 원래 의도가 무엇이었는지 불확실하면 추측하지
말고 물어봐라.

## 3. 오디오가 너무 투박함 — BGM 재작곡 + SFX 질감 추가 + 힐 효과음 신설

`src/systems/audio/backend.ts`의 `playSynthScore`/`getScoreProfile`
(BGM)과 `playSynthOneShot`(SFX, `SynthProfile.kind`가
`pad|pluck|pulse|noiseHit|sweepUp|sweepDown|chime` 6종뿐)을 확인해라.
지금은 단순 sine/triangle/square 파형 1~2겹으로만 되어 있다.

요청 톤: **웅장하고 비장하면서 점점 고조되는 느낌**. 구체적으로:

- **배경음악**: 현재 상태(state)별 코드/베이스/리드 진행을 다시
  작곡해라. 낮은 금관/현악 느낌의 지속음 레이어를 더하고, `battle-low`
  에서 `battle-high`로 갈수록 레이어가 쌓이며 긴장감이 고조되는 구조로
  만들어라. 지금처럼 단순 루프가 아니라 프레이즈가 전개되는 느낌이면
  좋겠다.
- **전투 타격음**: "서걱서걱"(칼/도끼가 베는 소리, 근접 공격/타격),
  "투곽투곽"(둔기/충격음, 원거리 명중이나 타워 공격 등), "으억/윽"
  (유닛 피격·사망 시 사람 신음 비슷한 느낌)이 나야 한다. 지금 `noiseHit`
  하나로 이 세 가지를 다 표현하고 있는데, 최소 이 세 카테고리는 서로
  다른 합성 방식(레이어링, 노이즈 쉐이핑, 피치 엔벨로프 등)으로
  구별되게 만들어라.
- **보급대 힐 효과음 신설**: "치리링" 하는 반짝이는 벨/차임 느낌의 SFX를
  새로 추가하고(`src/systems/audio/assetManifest.ts`에 새 SFX id 등록,
  예: `sfx.support.heal`), `LaneBattleScene.ts`의 `tickSupport()` 힐
  처리 분기(약 1403~1425행, 지금 `this.audio.playSfx(...)` 호출이
  아예 없음)에 실제로 연결해라.
- 시작하기 전에 **이 세션(Codex)에서 실제 오디오 파일을 생성할 수 있는
  도구를 쓸 수 있는지 먼저 확인해라.** 쓸 수 있으면 실제 생성 오디오
  자산을 우선 검토하고(그럴 경우 출처/라이선스를
  `assetManifest.ts`의 `licenseNote`에 반드시 기록), 못 쓰면 지금처럼
  Web Audio 합성 품질을 개선하는 쪽으로 가라 — 둘 다 안 되는 게
  확실하지 않으면 추측하지 말고 물어봐라.
- Audio Lab(`tools/audio-lab/`)으로 재생해보면서 비교 검증해라.

## 4. 키보드 전용 조작 제거 (모바일 대비, UI로 전부 대체)

`grep -rn "keyboard\.|addKey|keydown-" src/scenes src/ui`로 전수 조사한
결과, `src/ui/AudioSettingsPanel.ts`의 ESC/M/좌우화살표는 이미 클릭
버튼과 드래그 슬라이더가 다 있어서 문제없다(키보드는 추가 단축키일
뿐이니 그대로 둬도 됨). **진짜 문제는 `LaneBattleScene.ts`의 약 673행
`keydown-T`** — 지형 렌더 모드(`legacy/prototype/prototype-v2`)를
순환시키는 기능인데, 터치/클릭으로 접근할 UI가 전혀 없고 키보드가
유일한 경로다(URL 쿼리 파라미터로 우회 가능하지만 그것도 UI는 아님).

- 이게 사용자에게 노출될 실제 기능이면 화면에 탭 가능한 버튼(예: 설정
  패널 안에 "지형 모드" 토글)을 추가해라.
- 이게 개발/QA 전용 디버그 기능이면, 일반 플레이 빌드에서는 아예
  keydown 리스너를 등록하지 말고 디버그 플래그(예: URL 쿼리 파라미터
  존재 여부)로 게이트해서 실수로 남아있지 않게 해라.
- 어느 쪽인지 불확실하면 추측하지 말고 물어봐라.
- 이 김에 저장소 전체에서 "키보드가 유일한 접근 경로인 조작"이 더
  있는지 한 번 더 훑어보고, 있으면 같은 기준으로 정리해라. `main.ts`에
  등록 안 된 `DungeonScene.ts`의 WASD는 실제 게임과 무관하니 건드릴
  필요 없다.

## 5. 배경 화면이 여전히 한 장의 통짜 이미지 — 타일 조합형으로 확장

`docs/dev-wiki/terrain-rendering-plan.md`에 이미 정확한 진단과 권장안이
있다: 지금 `lane-battlefield-object-base-v4.png` 한 장이 depth 0에서
전체 플레이 영역을 담당하고 있고, 건물 footprint나 지면 점유 셀 같은
논리 지형 데이터가 없다. `docs/dev-wiki/terrain-prototype-validation.md`
→ `v2` → `v3` 검증 과정에서 하이브리드 타일 렌더링(원경 매트 배경 +
플레이 가능 구간 타일/데칼 + 논리 지형 그리드)을 이미 만들어봤고,
`terrain-prototype-v2-validation.md`가 `balanced / M` 프리셋을 기술
추천안으로 정리해뒀는데, **지금까지는 중앙 거점 8x8 패치에만
적용했고 전체 레인으로 확장한 적이 없다.** 첨부한 스크린샷에서 보이는
"배경과 타일 오브젝트가 분리되어 떠 보이는" 현상이 바로 이 미완성
상태다.

- `terrain-rendering-plan.md`의 권장안과 `terrain-prototype-v2-validation.md`
  의 `balanced / M` 추천을 기준으로, 지금까지 중앙 거점에만 적용한
  하이브리드 타일 구성을 **전체 플레이 가능 레인**으로 확장해라. 완전히
  새로 설계하지 말고 이미 검증된 접근을 확장하는 방향으로.
- 이전 단계들이 그랬듯, 확장 전후로 전투 규칙/스탯/시각적으로 무관한
  게임플레이 스냅샷이 동일하게 유지되는지 검증해라(이전
  `terrain-prototype-v2-validation.md`가 쓴 것과 같은 방식).
- 스코프가 예상보다 크면(예: 전체 맵 asset을 다시 만들어야 하는 수준),
  일단 어디까지 진행했는지와 남은 범위를 정리해서 보고하고 사용자
  판단을 받아라 — 끝까지 밀어붙이지 마라.

---

# 결과물

각 항목마다:
- 무엇을 확인했고 무엇을 바꿨는지
- 검증 방법과 결과(`npm run build`/`npm test`/Playwright 등)
- 확실하지 않아서 사용자에게 확인이 필요한 부분

`docs/dev-wiki/log.md`에 하네스 규칙대로 기록하고, 이 다섯 개는 서로
다른 우선순위/난이도를 가지니 순서대로 하나씩 끝내고 다음으로 넘어가는
편을 추천한다(전부 동시에 건드리면 회귀 원인 추적이 어려워진다).
```
